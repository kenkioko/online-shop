<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Items Display
    |--------------------------------------------------------------------------
    |
    | This options controls the way items are displayed in rows and columns.
    |
    */

    // Number of rows in the homepage (default = 5)
    'homepage_rows' => 5,

    // Colums per each row (default 4)
    'cols_per_row' => 4,

    // No of items in a row (default 8)
    'items_in_row' => 8,
];
