Models
------

Models belong in the ``App\Models`` namespace.
All models inherit the Laravel’s ``Illuminate\Database\Eloquent\Model`` class.

The ``\App\User`` Model is the Laravel's default user model.

For any extentions on the functions of the models use Laravel's facades
``Illuminate\Support\Facades``. within the namespace ``App\Facades``.


.. toctree::
    :maxdepth: 1
    :caption: Models Contents:


    models/user
    models/shop
    models/category
    models/item
    models/service
    models/order
    models/phone
    models/address
    models/ussd

